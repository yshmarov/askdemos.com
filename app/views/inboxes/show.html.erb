<%= render @inbox %>

<%= link_to 'Edit this inbox', edit_inbox_path(@inbox), class: 'mt-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium' %>
<div class="inline-block ml-2">
  <%= button_to 'Destroy this inbox', inbox_path(@inbox), method: :delete, class: 'mt-2 rounded-lg py-3 px-5 bg-gray-100 font-medium' %>
</div>
